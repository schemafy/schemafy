{
  "name": "backend",
  "private": true,
  "version": "0.0.1",
  "scripts": {
    "build": "cd ../.. && ./gradlew :core:build :domain:build",
    "dev": "cd ../.. && ./gradlew :core:bootRun",
    "lint": "echo 'No linter configured for backend'",
    "format": "cd ../.. && ./gradlew :core:spotlessApply :domain:spotlessApply",
    "format:check": "cd ../.. && ./gradlew :core:spotlessCheck :domain:spotlessCheck",
    "test": "cd ../.. && ./gradlew :core:test :domain:test"
  }
}
