{
  "name": "backend",
  "private": true,
  "version": "0.0.1",
  "scripts": {
    "build": "cd core && ./gradlew build",
    "dev": "cd core && ./gradlew bootRun",
    "lint": "echo 'No linter configured for backend'",
    "format": "cd core && ./gradlew spotlessApply",
    "format:check": "cd core && ./gradlew spotlessCheck",
    "test": "cd core && ./gradlew test"
  }
}
