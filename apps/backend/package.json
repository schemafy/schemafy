{
  "name": "backend",
  "private": true,
  "scripts": {
    "dev": "sh -c \"$(git rev-parse --show-toplevel)/gradlew -p core bootRun\"",
    "build": "sh -c \"$(git rev-parse --show-toplevel)/gradlew -p core build\"",
    "dev:infra": "podman compose up -d",
    "infra:up": "podman compose up -d",
    "infra:down": "podman compose down"
  }
}
