spring:
  config:
    activate:
      on-profile: server

management:
  endpoints:
    web:
      exposure:
        include:
          - health
          - info
  endpoint:
    health:
      probes:
        enabled: true
      show-details: never
      show-components: never
