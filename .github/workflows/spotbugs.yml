name: SpotBugs Code Quality Check

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

permissions:
  contents: read
  pull-requests: write

jobs:
  spotbugs:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          java-version: "21"
          distribution: "temurin"
          cache: "gradle"

      - name: Make gradlew executable
        run: chmod +x apps/backend/core/gradlew

      - name: Cache Gradle
        uses: actions/cache@v4
        with:
          path: |
            ~/.gradle/caches
            ~/.gradle/wrapper
          key: ${{ runner.os }}-gradle-${{ hashFiles('**/*.gradle*', '**/gradle-wrapper.properties') }}
          restore-keys: |
            ${{ runner.os }}-gradle-

      - name: Run SpotBugs analysis
        run: |
          cd apps/backend/core
          ./gradlew spotbugsMain --console=plain

      - name: Upload SpotBugs HTML report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: spotbugs-html-report
          path: apps/backend/core/build/reports/spotbugs/main.html
          retention-days: 30

      - name: Analyze SpotBugs results
        if: always()
        id: analyze
        run: |
          cd apps/backend/core
          if [ -f build/reports/spotbugs/main.xml ]; then
            HIGH_BUGS=$(grep -o 'priority="1"' build/reports/spotbugs/main.xml | wc -l || echo "0")
            MEDIUM_BUGS=$(grep -o 'priority="2"' build/reports/spotbugs/main.xml | wc -l || echo "0")
            LOW_BUGS=$(grep -o 'priority="3"' build/reports/spotbugs/main.xml | wc -l || echo "0")

            echo "high_bugs=$HIGH_BUGS" >> $GITHUB_OUTPUT
            echo "medium_bugs=$MEDIUM_BUGS" >> $GITHUB_OUTPUT
            echo "low_bugs=$LOW_BUGS" >> $GITHUB_OUTPUT

            echo "### 🔍 SpotBugs Analysis Results" >> $GITHUB_STEP_SUMMARY
            echo "" >> $GITHUB_STEP_SUMMARY
            echo "| Priority | Count |" >> $GITHUB_STEP_SUMMARY
            echo "|----------|-------|" >> $GITHUB_STEP_SUMMARY
            echo "| 🔴 High   | $HIGH_BUGS |" >> $GITHUB_STEP_SUMMARY
            echo "| 🟡 Medium | $MEDIUM_BUGS |" >> $GITHUB_STEP_SUMMARY
            echo "| 🟢 Low    | $LOW_BUGS |" >> $GITHUB_STEP_SUMMARY

            if [ "$HIGH_BUGS" -gt "0" ] || [ "$MEDIUM_BUGS" -gt "0" ]; then
              echo "" >> $GITHUB_STEP_SUMMARY
              echo "⚠️ **Action Required**: Please review and fix the high/medium priority bugs." >> $GITHUB_STEP_SUMMARY
              echo "::error::SpotBugs found $HIGH_BUGS high and $MEDIUM_BUGS medium priority bugs"
              exit 1
            else
              echo "" >> $GITHUB_STEP_SUMMARY
              echo "✅ **No critical bugs found!**" >> $GITHUB_STEP_SUMMARY
            fi
          else
            echo "::warning::SpotBugs report not found"
          fi

      - name: Comment PR with results
        if: always() && github.event_name == 'pull_request'
        uses: actions/github-script@v7
        with:
          script: |
            const fs = require('fs');
            const high = '${{ steps.analyze.outputs.high_bugs }}' || '0';
            const medium = '${{ steps.analyze.outputs.medium_bugs }}' || '0';
            const low = '${{ steps.analyze.outputs.low_bugs }}' || '0';

            const status = (parseInt(high) + parseInt(medium)) > 0 ? '❌' : '✅';
            const message = `## ${status} SpotBugs Code Quality Report

| Priority | Count |
|----------|-------|
| 🔴 High   | ${high} |
| 🟡 Medium | ${medium} |
| 🟢 Low    | ${low} |

${(parseInt(high) + parseInt(medium)) > 0
  ? '⚠️ **Please review and fix the high/medium priority bugs before merging.**'
  : '✅ **No critical bugs found!**'}

📊 [View detailed HTML report](https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }})
`;

            github.rest.issues.createComment({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: context.issue.number,
              body: message
            });
